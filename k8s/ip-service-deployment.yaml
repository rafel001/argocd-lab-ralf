apiVersion: apps/v1
kind: Deployment
metadata:
  name: ip-service
  labels:
    app: time-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ip-service
  template:
    metadata:
      labels:
        app: ip-service
    spec:
      containers:
      - name: ip-service
        image: rafel0/ip-service:d65cce78e0e82b9f16df253c56e660b40cc72ac8
        ports:
        - containerPort: 3001
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: ip-service
spec:
  selector:
    app: ip-service
  ports:
  - port: 80
    targetPort: 3000
